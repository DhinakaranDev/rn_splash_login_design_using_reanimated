import { StyleSheet, Text, View } from "react-native";
import React from "react";

type Props = {};

const Loder = (props: Props) => {
  return (
    <View>
      <Text>Loder</Text>
    </View>
  );
};

export default Loder;

const styles = StyleSheet.create({});
